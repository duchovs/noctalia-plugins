# This file is auto-generated. Do not edit manually.
# Do not include it in your commit.
# Noctalia plugins catalog.
# Index of every plugin this source ships: the minimum needed to render, search,
# and compat-check the list. The per-plugin plugin.toml stays authoritative; the
# host re-reads it on enable. Keep one [[plugin]] row per plugin subdirectory.
# A [[plugin.release]] row names an older revision for a Noctalia below the tip's
# plugin_api, so an older release stays installable instead of the plugin vanishing.
# On every row, updated_at dates the commit that first shipped that row's version.

[[plugin]]
id = "duchovs/bitdjinn"
name = "BitDjinn (local fork)"
version = "1.2.0"
updated_at = 1788928483
added_at = 1788809688
author = "duchovs"
license = "MIT"
icon = "currency-bitcoin"
description = "Real-time cryptocurrency ticker, trend charts, and blockchain balance monitoring with transaction alerts."
plugin_api = 19
tags = ["utility", "indicator", "network", "panel", "bar"]

[[plugin]]
id = "duchovs/notes"
name = "Notes (local fork)"
version = "1.2.0"
updated_at = 1789002247
added_at = 1789002247
author = "duchovs"
license = "MIT"
icon = "notes"
description = "Quick markdown notes in a full-height side panel, with a rendered preview you click into to edit."
plugin_api = 21
tags = ["productivity"]

[[plugin.release]]
plugin_api = 9
version = "1.0.5"
updated_at = 1789001428
rev = "d38def4a520bee49744520b2142bb8b524fa7bda"

[[plugin]]
id = "duchovs/claude-code"
name = "Claude Code"
version = "1.2.0"
updated_at = 1789020812
added_at = 1789016832
author = "duchovs"
license = "MIT"
icon = "sparkles"
description = "Interactive Claude Code console: a persistent streaming session, tool and thinking transcript, and plan usage."
plugin_api = 24
tags = ["ai", "development", "productivity", "bar", "panel", "service"]

[[plugin]]
id = "duchovs/tasks"
name = "Tasks"
version = "2.0.2"
updated_at = 1789285561
added_at = 1789141882
author = "duchovs"
license = "MIT"
icon = "checklist"
description = "Nextcloud Tasks lists synced over CalDAV, with per-list color coding, filters and quick capture."
plugin_api = 28
tags = ["productivity", "network", "panel", "bar", "launcher"]
